# CRCON QQ Bot Dependencies
# 更灵活的版本约束以避免依赖冲突

# 核心框架 - 使用更宽松的版本范围
nonebot2>=2.0.0,<3.0.0
nonebot-adapter-onebot>=2.0.0,<3.0.0

# 可选插件
nonebot-plugin-apscheduler>=0.3.0

# HTTP 客户端
requests>=2.25.0
aiohttp>=3.7.0

# 配置管理 - 关键：使用兼容的 Pydantic 版本
pydantic>=1.8.0,<2.0.0
python-dotenv>=0.19.0

# 日志
loguru>=0.6.0